apiVersion: v1
kind: Service
metadata:
  name: worker-service
spec:
  selector:
    app: worker
  ports:
    - protocol: TCP
      port: {{ .Values.worker-backend.ports.port }}
      targetPort: {{ .Values.worker-backend.ports.containerPort }}
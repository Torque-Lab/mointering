apiVersion: v1
kind: Service
metadata:
  name: express-service
spec:
  selector:
    app: express
  ports:
    - protocol: TCP
      port: {{ .Values.express.ports[0].port }}
      targetPort: {{ .Values.express.ports[1].containerPort }}
  type: ClusterIP

